package day5;

public class HomeworkTen {
    public static void main(String[] args) {
        double rectangleLength = 12;
        double rectangleWidth = 24;
        double area = rectangleLength * rectangleWidth;
        double perimeter = (rectangleLength*2) + (rectangleWidth*2);
        System.out.println("For a rectangle with a length of " + rectangleLength + " units and a width of " + rectangleWidth + " units the area is " + area + "units^2 and the perimeter is " + perimeter + "units");
    }
}
